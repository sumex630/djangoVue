import { routes } from '@/router/routers.js'
import { getMainmenu } from '@/lib/util'

const state = {
	routers: routes,
	mainMenuList: [],
	sideMenuList: [],
}

const mutations = {
	//
}
const actions = {
	//
}

export default {
	state,
	mutations,
	actions,
}